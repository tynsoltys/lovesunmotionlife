import React from "react"
import { graphql } from "gatsby"
import Layout from "../components/layout"

// data.prismic.allActivitys.edges[0].node
export default function FAQPage({}) {
  return <Layout ulad={ulad} pageType="faq"></Layout>
}
